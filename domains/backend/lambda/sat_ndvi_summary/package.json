{
  "name": "sat_ndvi_summary",
  "version": "1.0.0",
  "description": "Lambda function to fetch ndvi data",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf ./build.zip && npm install && zip -r build.zip ."
  },
  "dependencies": {
    "geotiff": "^2.1.3",
    "node-fetch": "^2.6.1"
  }
}
